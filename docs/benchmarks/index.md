# Benchmarks

Detectiv keeps benchmark-specific data loading and upstream integrations in
`detectiv.benchmarks`. Benchmark datasets are local inputs: they are not
committed to this repository and are not required to install Detectiv.

## Available Benchmarks

- [TSB-AD](tsb-ad.md): time-series anomaly detection benchmark datasets and
  evaluation utilities.
