# Reproducibility API

Call `configure_reproducibility` before constructing randomly initialized
models. Pass its returned settings to TS2I materialization and scenarios so one
configuration controls and documents the complete experiment.

::: detectiv.reproducibility
