# Runtime Device Selection

`ComputeDevice` provides typed names for automatic, CPU, CUDA, and MPS
selection. Indexed devices such as `"cuda:1"` remain supported as strings.

::: detectiv.models.runtime
